# Credentials — never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/

# Shopify auth token (persisted by /shopify/callback)
data/shopify_token.json

# Rate-log SQLite DB (runtime data — lives on the Render disk in prod)
*.db

# Local virtualenv
.venv/
venv/
